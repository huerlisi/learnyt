.position= form.text_field :position
.score= form.text_field :score
= form.fields_for :answer do |a|
  = a.text_field :title, 'data-autocomplete' => questions_url(:format => :json)
  %p.description= a.text_area :description, :rows => 4
  %p.remarks= a.text_area :remarks, :rows => 4
